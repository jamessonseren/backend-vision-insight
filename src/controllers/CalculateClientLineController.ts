// import { Request, Response } from "express";
// import { CalculateClientLineService } from "../services/CalculateClientLineService";
// class CalculateClientLineController{
//     async handle(req: Request, res: Response){

//         const calculateClientLine = new CalculateClientLineService()

//         const clientId = req.query.clientId as string

//         const clientLine = await calculateClientLine.execute(clientId)

//         return res.json(clientLine)
        
//     }
// }

// export { CalculateClientLineController }